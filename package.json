{
  "name": "nodiator",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "postinstall": "husky install",
    "build": "lerna run build --stream",
    "lint:staged": "scripts/lint-staged.sh",
    "test": "lerna run test --stream --since HEAD --exclude-dependents",
    "test:e2e": "lerna run test:e2e --stream --since HEAD",
    "test:cov": "jest --config jest-units.config.json --coverage",
    "precommit": "concurrently \"yarn:lint:staged\" \"yarn:test\" \"yarn:test:e2e\""
  },
  "devDependencies": {
    "@commitlint/cli": "^17.0.3",
    "@commitlint/config-conventional": "^17.0.3",
    "@types/jest": "^27.4.1",
    "@typescript-eslint/eslint-plugin": "^5.17.0",
    "@typescript-eslint/parser": "^5.17.0",
    "commitizen": "^4.2.5",
    "concurrently": "^7.3.0",
    "cz-conventional-changelog": "^3.3.0",
    "eslint": "^8.11.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.0.0",
    "husky": "^8.0.0",
    "jest": "^27.5.1",
    "lerna": "^5.3.0",
    "prettier": "^2.5.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.1.4",
    "typescript": "^4.7.4"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "packageManager": "yarn@3.2.2",
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
